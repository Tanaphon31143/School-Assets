<x-app-layout><x-slot name="header"><h2 class="font-semibold text-xl">รายงานครุภัณฑ์</h2></x-slot><div class="py-8 max-w-7xl mx-auto px-4"><div class="mb-4 space-x-2"><a class="bg-green-600 text-white px-4 py-2 rounded" href="{{ route('reports.csv') }}">Export Excel</a><a class="bg-red-600 text-white px-4 py-2 rounded" href="{{ route('reports.pdf') }}">Export PDF</a></div><div class="grid md:grid-cols-3 gap-4 mb-6"><div class="bg-white p-5 rounded shadow"><div class="text-gray-500">จำนวนรายการ</div><div class="text-2xl font-bold">{{ $equipment->count() }}</div></div><div class="bg-white p-5 rounded shadow"><div class="text-gray-500">จำนวนชิ้นทั้งหมด</div><div class="text-2xl font-bold">{{ $equipment->sum('quantity') }}</div></div><div class="bg-white p-5 rounded shadow"><div class="text-gray-500">มูลค่ารวม</div><div class="text-2xl font-bold">{{ number_format($totalValue,2) }}</div></div></div><div class="grid md:grid-cols-2 gap-6"><div class="bg-white p-5 rounded shadow"><h3 class="font-bold mb-3">ตามสถานะ</h3>@foreach($byStatus as $status=>$count)<div class="flex justify-between border-b py-2"><span>{{ $status }}</span><b>{{ $count }}</b></div>@endforeach</div><div class="bg-white p-5 rounded shadow"><h3 class="font-bold mb-3">ตามประเภท</h3>@foreach($byCategory as $category=>$count)<div class="flex justify-between border-b py-2"><span>{{ $category }}</span><b>{{ $count }}</b></div>@endforeach</div></div></div></x-app-layout>
