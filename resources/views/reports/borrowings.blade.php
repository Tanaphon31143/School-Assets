<x-app-layout><x-slot name="header"><h2 class="font-semibold text-xl">รายงานประวัติการยืม–คืน</h2></x-slot><div class="py-8 max-w-7xl mx-auto px-4"><form class="ui-card p-4 mb-6 flex flex-wrap gap-2"><select name="status" class="border rounded-lg px-3 py-2"><option value="">ทุกสถานะ</option>@foreach(['pending','approved','borrowed','returned','rejected','overdue'] as $s)<option value="{{ $s }}" @selected(request('status')===$s)>{{ $s }}</option>@endforeach</select><input type="date" name="from" value="{{ request('from') }}" class="border rounded-lg px-3 py-2"><input type="date" name="to" value="{{ request('to') }}" class="border rounded-lg px-3 py-2"><button class="ui-button-primary">กรองรายงาน</button><a href="{{ route('reports.borrowings') }}" class="ui-button-secondary">ล้าง</a></form><div class="grid grid-cols-2 md:grid-cols-6 gap-3 mb-6">@foreach(['pending','approved','borrowed','returned','rejected','overdue'] as $status)<div class="ui-card p-4"><div class="text-slate-500 text-sm">{{ $status }}</div><div class="text-2xl font-bold">{{ $summary[$status]??0 }}</div></div>@endforeach</div><div class="ui-card overflow-x-auto"><table class="min-w-full"><thead><tr class="border-b text-left text-slate-500"><th class="p-3">วันที่ยืม</th><th class="p-3">ครุภัณฑ์</th><th class="p-3">ผู้ยืม</th><th class="p-3">กำหนดคืน</th><th class="p-3">คืนจริง</th><th class="p-3">สถานะ</th></tr></thead><tbody>@forelse($borrowings as $b)<tr class="border-b border-slate-100"><td class="p-3">{{ $b->borrow_date?->format('d/m/Y') }}</td><td class="p-3">{{ $b->equipment->code }} - {{ $b->equipment->name }}</td><td class="p-3">{{ $b->borrower->name }}</td><td class="p-3">{{ $b->expected_return_date?->format('d/m/Y') }}</td><td class="p-3">{{ $b->actual_return_date?->format('d/m/Y') ?? '-' }}</td><td class="p-3"><span class="px-2.5 py-1 rounded-full text-xs bg-slate-100">{{ $b->status }}</span></td></tr>@empty<tr><td colspan="6" class="p-6 text-center text-slate-400">ไม่พบข้อมูล</td></tr>@endforelse</tbody></table></div><div class="mt-5">{{ $borrowings->links() }}</div></div></x-app-layout>
