<x-app-layout><x-slot name="header"><h2 class="font-semibold text-xl">พิมพ์ QR Code ครุภัณฑ์</h2></x-slot><div class="py-8 max-w-7xl mx-auto px-4"><form method="GET" action="{{ route('reports.qr-print') }}"><div class="ui-card p-4 mb-6"><div class="flex justify-between items-center mb-3"><div><h3 class="font-bold">เลือกครุภัณฑ์</h3><p class="text-sm text-slate-500">เลือกเฉพาะรายการที่ต้องการพิมพ์</p></div><label class="text-sm text-blue-600"><input type="checkbox" id="select-all" class="rounded mr-1"> เลือกทั้งหมด</label></div><div class="grid md:grid-cols-3 gap-2 max-h-72 overflow-y-auto">@foreach($allEquipment as $item)<label class="flex items-center gap-2 p-2 rounded hover:bg-slate-50"><input type="checkbox" name="ids[]" value="{{ $item->id }}" class="qr-item rounded"><span>{{ $item->code }} - {{ $item->name }}</span></label>@endforeach</div><button class="ui-button-primary mt-4">แสดง QR ที่เลือก</button></div></form>@if($equipment->count())<div class="grid grid-cols-2 md:grid-cols-4 gap-6">@foreach($equipment as $item)<div class="ui-card p-4 text-center"><img class="mx-auto w-40 h-40" src="{{ route('equipment.qr',$item) }}" alt="QR {{ $item->code }}"><div class="font-semibold mt-2">{{ $item->code }}</div><div class="text-sm text-slate-500">{{ $item->name }}</div></div>@endforeach</div><button onclick="window.print()" class="mt-6 ui-button-primary">พิมพ์</button>@else<div class="ui-card p-8 text-center text-slate-500">ยังไม่ได้เลือกรายการ</div>@endif</div><script>document.getElementById('select-all')?.addEventListener('change',e=>document.querySelectorAll('.qr-item').forEach(i=>i.checked=e.target.checked));</script></x-app-layout>
