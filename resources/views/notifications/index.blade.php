<x-app-layout><x-slot name="header"><div class="flex justify-between items-center"><h2 class="font-semibold text-xl">การแจ้งเตือน</h2><form method="POST" action="{{ route('notifications.read-all') }}">@csrf @method('PATCH')<button class="text-sm text-blue-600">ทำเครื่องหมายอ่านทั้งหมด</button></form></div></x-slot><div class="py-8 max-w-3xl mx-auto px-4"><div class="bg-white rounded shadow divide-y">@forelse($notifications as $notification)<div class="p-4 flex justify-between gap-4 {{ $notification->read_at?'':'bg-blue-50' }}"><div>{{ $notification->data['message'] ?? 'การแจ้งเตือน' }}<div class="text-xs text-gray-500">{{ $notification->created_at->diffForHumans() }}</div></div>@if(!$notification->read_at)<form method="POST" action="{{ route('notifications.read',$notification->id) }}">@csrf @method('PATCH')<button class="text-sm text-blue-600 whitespace-nowrap">อ่านแล้ว</button></form>@endif</div>@empty<div class="p-5">ยังไม่มีการแจ้งเตือน</div>@endforelse</div>{{ $notifications->links() }}</div></x-app-layout>
