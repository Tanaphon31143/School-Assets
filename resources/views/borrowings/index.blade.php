<x-app-layout>
    <x-slot name="header"><p class="text-xs font-medium tracking-wide text-slate-400">การใช้งานครุภัณฑ์</p><h2 class="mt-1 text-2xl font-bold tracking-tight text-slate-900">รายการยืม–คืน</h2></x-slot>
    <div class="px-4 py-8 sm:px-8">
        @if (session('success'))<div class="mb-5 rounded-xl border border-emerald-200 bg-emerald-50 px-4 py-3 text-sm font-medium text-emerald-700">{{ session('success') }}</div>@endif
        <div class="mb-6 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><h1 class="text-xl font-bold text-slate-900">ติดตามรายการยืมครุภัณฑ์</h1><p class="mt-1 text-sm text-slate-500">ตรวจสอบสถานะคำขอและการคืนครุภัณฑ์</p></div><form class="flex gap-2"><select name="status" class="min-h-11 rounded-xl border-slate-200 px-3 text-sm"><option value="">ทุกสถานะ</option>@foreach(['pending'=>'รออนุมัติ','approved'=>'อนุมัติแล้ว','rejected'=>'ไม่อนุมัติ','borrowed'=>'กำลังยืม','returned'=>'คืนแล้ว','overdue'=>'เกินกำหนด'] as $value=>$label)<option value="{{ $value }}" @selected(request('status') === $value)>{{ $label }}</option>@endforeach</select><button class="ui-button-primary">กรอง</button></form></div>
        <div class="ui-card overflow-hidden"><div class="overflow-x-auto"><table class="min-w-full text-sm"><thead><tr class="border-b border-slate-100 text-left"><th class="p-4">ครุภัณฑ์</th><th class="p-4">ผู้ยืม</th><th class="p-4">กำหนดคืน</th><th class="p-4">สถานะ</th><th class="p-4">ดำเนินการ</th></tr></thead><tbody>@forelse($borrowings as $b)<tr class="border-b border-slate-100 last:border-0"><td class="p-4"><p class="font-semibold text-slate-800">{{ $b->equipment->name }}</p><p class="mt-1 text-xs text-slate-400">{{ $b->equipment->code }}</p></td><td class="p-4 text-slate-600">{{ $b->borrower->name }}</td><td class="p-4 text-slate-500">{{ $b->expected_return_date?->format('d/m/Y') ?? '-' }}</td><td class="p-4"><span class="rounded-full px-3 py-1 text-xs font-semibold {{ match($b->status){'pending'=>'bg-amber-50 text-amber-700','approved'=>'bg-blue-50 text-blue-700','borrowed'=>'bg-indigo-50 text-indigo-700','returned'=>'bg-emerald-50 text-emerald-700','rejected'=>'bg-red-50 text-red-700','overdue'=>'bg-rose-50 text-rose-700',default=>'bg-slate-100 text-slate-600'} }}">{{ ['pending'=>'รออนุมัติ','approved'=>'อนุมัติแล้ว','borrowed'=>'กำลังยืม','returned'=>'คืนแล้ว','rejected'=>'ไม่อนุมัติ','overdue'=>'เกินกำหนด'][$b->status] ?? $b->status }}</span></td><td class="p-4"><div class="flex flex-wrap gap-2">@if(in_array($b->status,['pending','approved']) && auth()->user()->hasAnyRole(['admin','teacher']))<form method="POST" action="{{ route('borrowings.approve',$b) }}">@csrf @method('PATCH')<button class="rounded-lg bg-emerald-50 px-3 py-2 text-xs font-semibold text-emerald-700 hover:bg-emerald-100">อนุมัติ</button></form><form method="POST" action="{{ route('borrowings.reject',$b) }}">@csrf @method('PATCH')<input type="hidden" name="approval_notes" value="ไม่ผ่านการอนุมัติ"><button class="rounded-lg bg-red-50 px-3 py-2 text-xs font-semibold text-red-700 hover:bg-red-100">ปฏิเสธ</button></form>@endif @if(in_array($b->status,['approved','borrowed']) && (auth()->id()===$b->user_id||auth()->user()->hasRole('admin')))<form method="POST" action="{{ route('borrowings.return',$b) }}">@csrf @method('PATCH')<button class="rounded-lg bg-blue-50 px-3 py-2 text-xs font-semibold text-blue-700 hover:bg-blue-100">บันทึกคืน</button></form>@endif</div></td></tr>@empty<tr><td colspan="5" class="p-10 text-center text-slate-400">ยังไม่มีรายการยืม–คืน</td></tr>@endforelse</tbody></table></div></div><div class="mt-5">{{ $borrowings->onEachSide(1)->links('vendor.pagination.minimal') }}</div>
    </div>
</x-app-layout>
