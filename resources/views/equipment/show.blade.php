<x-app-layout><x-slot name="header"><h2 class="font-semibold text-xl">{{ $equipment->name }}</h2></x-slot><div class="py-8 max-w-3xl mx-auto px-4"><div class="bg-white rounded shadow p-6 space-y-2"><p><b>รหัส:</b> {{ $equipment->code }}</p><p><b>สถานะ:</b> {{ $equipment->status }}</p><p><b>สถานที่:</b> {{ $equipment->location?->name ?? '-' }}</p><p><b>จำนวน:</b> {{ $equipment->quantity }} {{ $equipment->unit }}</p>@if($equipment->status==='available')<form method="POST" action="{{ route('borrowings.store') }}" class="border-t pt-4 mt-4 space-y-2">@csrf<input type="hidden" name="equipment_id" value="{{ $equipment->id }}"><input type="number" name="quantity" value="1" min="1" max="{{ $equipment->quantity }}" class="border rounded"><input type="date" name="expected_return_date" required class="border rounded"><input name="purpose" placeholder="วัตถุประสงค์" class="border rounded px-2"><button class="bg-green-600 text-white px-4 py-2 rounded">ส่งคำขอยืม</button></form>@endif</div></div></x-app-layout>
