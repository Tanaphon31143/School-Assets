<x-app-layout><x-slot name="header"><h2 class="font-semibold text-xl">รายการครุภัณฑ์</h2></x-slot><div class="py-8 max-w-7xl mx-auto px-4"><form class="mb-5 flex flex-wrap gap-2"><input name="search" value="{{ request('search') }}" placeholder="ค้นหาชื่อหรือรหัส" class="border rounded-lg px-3 py-2 flex-1 min-w-52"><select name="status" class="border rounded-lg px-3 py-2"><option value="">ทุกสถานะ</option>@foreach(['available'=>'พร้อมใช้งาน','borrowed'=>'กำลังยืม','maintenance'=>'กำลังซ่อม','damaged'=>'ชำรุด','disposed'=>'จำหน่ายแล้ว'] as $value=>$label)<option value="{{ $value }}" @selected(request('status')===$value)>{{ $label }}</option>@endforeach</select><button class="ui-button-primary">ค้นหา</button>@if(request()->hasAny(['search','status']))<a href="{{ route('equipment.index') }}" class="ui-button-secondary">ล้างตัวกรอง</a>@endif</form><div class="ui-card overflow-x-auto"><table class="min-w-full"><thead><tr class="text-left border-b border-slate-200"><th class="p-4">รหัส</th><th class="p-4">ชื่อครุภัณฑ์</th><th class="p-4">ประเภท</th><th class="p-4">สถานที่</th><th class="p-4">สถานะ</th><th class="p-4"></th></tr></thead><tbody>@forelse($equipment as $item)<tr class="border-b border-slate-100 hover:bg-slate-50"><td class="p-4 font-medium">{{ $item->code }}</td><td class="p-4">{{ $item->name }}</td><td class="p-4 text-slate-500">{{ $item->category?->name ?? '-' }}</td><td class="p-4 text-slate-500">{{ $item->location?->name ?? '-' }}</td><td class="p-4"><span class="inline-flex px-3 py-1 rounded-full text-xs font-semibold {{ match($item->status){'available'=>'bg-emerald-100 text-emerald-700','borrowed'=>'bg-blue-100 text-blue-700','maintenance'=>'bg-amber-100 text-amber-700','damaged'=>'bg-red-100 text-red-700',default=>'bg-slate-100 text-slate-700'} }}">{{ ['available'=>'พร้อมใช้งาน','borrowed'=>'กำลังยืม','maintenance'=>'กำลังซ่อม','damaged'=>'ชำรุด','disposed'=>'จำหน่ายแล้ว'][$item->status] ?? $item->status }}</span></td><td class="p-4"><a href="{{ route('equipment.show',$item) }}" class="table-action-view">ดูรายละเอียด</a></td></tr>@empty<tr><td colspan="6" class="p-8 text-center text-slate-500">ไม่พบข้อมูลครุภัณฑ์</td></tr>@endforelse</tbody></table></div><div class="mt-5">{{ $equipment->onEachSide(1)->links('vendor.pagination.minimal') }}</div></div></x-app-layout>
