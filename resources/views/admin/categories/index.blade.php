<x-app-layout><x-slot name="header"><h2 class="font-semibold text-xl">จัดการประเภทครุภัณฑ์</h2></x-slot><div class="py-8 max-w-6xl mx-auto px-4">@if(session('success'))<div class="bg-green-100 p-3 mb-4 rounded">{{ session('success') }}</div>@endif @if(session('error'))<div class="bg-red-100 p-3 mb-4 rounded">{{ session('error') }}</div>@endif<div class="mb-4"><a href="{{ route('admin.categories.create') }}" class="bg-blue-600 text-white px-4 py-2 rounded">เพิ่มประเภท</a></div><div class="bg-white rounded shadow overflow-x-auto"><table class="min-w-full"><thead><tr class="border-b text-left"><th class="p-3">ชื่อ</th><th class="p-3">รหัส</th><th class="p-3">จำนวนครุภัณฑ์</th><th class="p-3">จัดการ</th></tr></thead><tbody>@forelse($categories as $category)<tr class="border-b"><td class="p-3">{{ $category->name }}</td><td class="p-3">{{ $category->code }}</td><td class="p-3">{{ $category->equipment_count }}</td><td class="p-3 flex gap-2"><a class="text-blue-600" href="{{ route('admin.categories.edit',$category) }}">แก้ไข</a><form method="POST" action="{{ route('admin.categories.destroy',$category) }}" onsubmit="return confirm('ยืนยันการลบ?')">@csrf @method('DELETE')<button class="text-red-600">ลบ</button></form></td></tr>@empty<tr><td colspan="4" class="p-4">ยังไม่มีข้อมูล</td></tr>@endforelse</tbody></table></div>{{ $categories->links() }}</div></x-app-layout>
