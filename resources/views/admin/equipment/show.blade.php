<x-app-layout><x-slot name="header"><h2 class="font-semibold text-xl">รายละเอียดครุภัณฑ์</h2></x-slot><div class="py-8 max-w-5xl mx-auto px-4"><div class="ui-card overflow-hidden"><div class="bg-slate-50 p-6 flex flex-col md:flex-row gap-6 items-start">@if($equipment->image)<img src="{{ Storage::url($equipment->image) }}" class="w-48 h-48 rounded-xl object-cover shadow-sm">@else<div class="w-48 h-48 rounded-xl bg-white border border-slate-200 flex items-center justify-center text-5xl text-slate-300">▣</div>@endif<div><div class="text-sm text-slate-500">{{ $equipment->code }}</div><h1 class="text-2xl font-bold text-slate-900 mt-1">{{ $equipment->name }}</h1><p class="text-slate-500 mt-2">{{ $equipment->brand }} {{ $equipment->model }}</p><span class="inline-flex mt-4 px-3 py-1 rounded-full text-sm font-semibold {{ match($equipment->status){'available'=>'bg-emerald-100 text-emerald-700','borrowed'=>'bg-blue-100 text-blue-700','maintenance'=>'bg-amber-100 text-amber-700','damaged'=>'bg-red-100 text-red-700',default=>'bg-slate-100 text-slate-700'} }}">{{ $equipment->status }}</span></div></div><div class="p-6 grid md:grid-cols-2 gap-4 text-sm"><div><span class="text-slate-500">ประเภท</span><div class="font-medium">{{ $equipment->category?->name }}</div></div><div><span class="text-slate-500">สถานที่</span><div class="font-medium">{{ $equipment->location?->name ?? '-' }}</div></div><div><span class="text-slate-500">สภาพ</span><div class="font-medium">{{ $equipment->condition }}</div></div><div><span class="text-slate-500">จำนวน</span><div class="font-medium">{{ $equipment->quantity }} {{ $equipment->unit }}</div></div><div><span class="text-slate-500">ราคาซื้อ</span><div class="font-medium">{{ number_format($equipment->purchase_price,2) }}</div></div><div><span class="text-slate-500">หมดประกัน</span><div class="font-medium">{{ $equipment->warranty_expire_date?->format('d/m/Y') ?? '-' }}</div></div></div><div class="p-6 border-t border-slate-100 flex flex-wrap gap-3"><a href="{{ route('equipment.qr',$equipment) }}" target="_blank" class="ui-button-secondary">QR Code</a><a href="{{ route('admin.equipment.edit',$equipment) }}" class="ui-button-primary">แก้ไขข้อมูล</a></div></div></div></x-app-layout>
