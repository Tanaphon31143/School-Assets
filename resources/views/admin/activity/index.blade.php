<x-app-layout><x-slot name="header"><h2 class="font-semibold text-xl">ประวัติการเปลี่ยนแปลงระบบ</h2></x-slot><div class="py-8 max-w-7xl mx-auto px-4"><form class="mb-4"><input name="search" value="{{ request('search') }}" placeholder="ค้นหาประวัติ" class="border rounded px-3 py-2"><button class="bg-gray-700 text-white px-4 py-2 rounded">ค้นหา</button></form><div class="bg-white rounded shadow overflow-x-auto"><table class="min-w-full"><thead><tr class="border-b text-left"><th class="p-3">เวลา</th><th class="p-3">ผู้ดำเนินการ</th><th class="p-3">การกระทำ</th><th class="p-3">รายการ</th><th class="p-3">รายละเอียด</th></tr></thead><tbody>@forelse($activities as $activity)<tr class="border-b"><td class="p-3 whitespace-nowrap">{{ $activity->created_at->format('d/m/Y H:i') }}</td><td class="p-3">{{ $activity->causer?->name ?? 'ระบบ' }}</td><td class="p-3">{{ $activity->event ?? '-' }}</td><td class="p-3">{{ class_basename($activity->subject_type ?? '') }} #{{ $activity->subject_id }}</td><td class="p-3">{{ $activity->description }}</td></tr>@empty<tr><td colspan="5" class="p-4">ยังไม่มีประวัติ</td></tr>@endforelse</tbody></table></div>{{ $activities->links() }}</div></x-app-layout>
