<x-app-layout><x-slot name="header"><h2 class="font-semibold text-xl">จำหน่าย/ตัดจำหน่ายครุภัณฑ์</h2></x-slot><div class="py-8 max-w-7xl mx-auto px-4">@if(session('success'))<div class="bg-green-100 p-3 mb-4 rounded">{{ session('success') }}</div>@endif<div class="mb-4"><a href="{{ route('admin.disposals.create') }}" class="inline-flex items-center min-h-12 px-5 py-3 rounded-lg bg-red-600 text-white font-semibold shadow hover:bg-red-700">+ บันทึกการจำหน่าย</a></div><div class="bg-white rounded shadow overflow-x-auto"><table class="min-w-full"><thead><tr class="border-b text-left"><th class="p-3">วันที่</th><th class="p-3">ครุภัณฑ์</th><th class="p-3">เหตุผล</th><th class="p-3">วิธีจำหน่าย</th><th class="p-3">ผู้อนุมัติ</th></tr></thead><tbody>@forelse($disposals as $d)<tr class="border-b"><td class="p-3">{{ $d->disposal_date?->format('d/m/Y') }}</td><td class="p-3">{{ $d->equipment->code }} - {{ $d->equipment->name }}</td><td class="p-3">{{ $d->reason }}</td><td class="p-3">{{ $d->disposal_method }}</td><td class="p-3">{{ $d->approver?->name ?? '-' }}</td></tr>@empty<tr><td colspan="5" class="p-4">ยังไม่มีรายการจำหน่าย</td></tr>@endforelse</tbody></table></div>{{ $disposals->links() }}</div></x-app-layout>
