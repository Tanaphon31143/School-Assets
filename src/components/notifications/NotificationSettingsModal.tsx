import styles from "@/app/notifications/notifications.module.css";
export function NotificationSettingsModal({open,onClose,settings,onChange}:{open:boolean;onClose:()=>void;settings:Record<string,boolean>;onChange:(key:string)=>void}){if(!open)return null;return <div className={styles.overlay} onMouseDown={e=>e.target===e.currentTarget&&onClose()}><section className={styles.modal} role="dialog" aria-modal="true" aria-labelledby="notification-settings"><div><h2 id="notification-settings">ตั้งค่าการแจ้งเตือน</h2><button onClick={onClose} aria-label="ปิด">×</button></div><p>เลือกประเภทการแจ้งเตือนที่ต้องการรับ</p>{[["borrowing","ยืม-คืน"],["maintenance","แจ้งซ่อม"],["asset","ครุภัณฑ์"],["user","ผู้ใช้งาน"]].map(([key,label])=><label key={key}><input type="checkbox" checked={settings[key]} onChange={()=>onChange(key)}/>{label}</label>)}<footer><button className="primary" onClick={onClose}>บันทึกการตั้งค่า</button></footer></section></div>}
